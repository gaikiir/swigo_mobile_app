// CategoryModel defines a category with ID, name, and image path.
// Used for consistent category handling across the app.
class CategoryModel {
  final String id, name, image;
  CategoryModel({required this.id, required this.name, required this.image});
}